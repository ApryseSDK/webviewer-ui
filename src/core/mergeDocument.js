/**
 * https://www.pdftron.com/api/web/CoreControls.Document.html#mergeDocument__anchor
 */

export default (documentToMerge, position) => window.docViewer.getDocument().mergeDocument(documentToMerge, position);
